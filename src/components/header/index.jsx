import React from 'react';

const Index = ({logo,name}) => {
    return (
        <header className="nav bg-danger header" >
            <p>{name}</p>
            
        </header>
    );
};

export default Index;